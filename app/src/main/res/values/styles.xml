<resources>


    <style name="DopeTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:windowNoTitle">true</item>
        <!--We will be using the toolbar so no need to show ActionBar-->
        <item name="android:windowActionBar">false</item>
        <!-- Set theme colors from http://www.google.com/design/spec/style/color.html#color-color-palette-->
        <!-- colorPrimary is used for the default action bar background -->
    </style>

    <style name="DopeButton" parent="Base.Widget.AppCompat.Button">
        <item name="android:gravity">center</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>

        <item name="android:background">@drawable/button_bg_selector</item>

        <item name="android:textColor">@color/black</item>

        <item name="android:paddingTop">@dimen/padding.top.bottom</item>
        <item name="android:paddingBottom">@dimen/padding.top.bottom</item>
        <item name="android:paddingLeft">@dimen/padding.left.right</item>
        <item name="android:paddingRight">@dimen/padding.left.right</item>


        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:minWidth">@dimen/button.dope.min.width</item>

    </style>


    <style name="TigoAdTextView" parent="@android:style/TextAppearance.Medium">
        <item name="android:gravity">left</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>

        <item name="android:background">@color/blue.t</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/tigoad.text.size</item>

        <item name="android:padding">@dimen/tigoad.padding</item>

        <item name="android:maxHeight">@dimen/tigoad.height.max</item>

    </style>



</resources>
