<android.support.v7.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/toolbar_save_activity"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="?android:attr/actionBarSize"
    android:layout_gravity="center_horizontal|top"
    android:padding="4dp"
    android:background="#53000000">

    <com.mj.instashusha.utils.DopeTextView
        android:id="@+id/tv_appname"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:text="@string/app_name"
        android:paddingLeft="@dimen/padding.left.right"
        android:paddingBottom="@dimen/padding.top.bottom"
        android:paddingTop="@dimen/padding.top.bottom"
        android:textSize="@dimen/text.size.toolbar"
        android:gravity="center_vertical"
        />

    <ImageView
        android:id="@+id/toolbar_action_settings"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical|right"
        android:layout_margin="4dp"
        android:src="@drawable/ic_action_home"
        />

</android.support.v7.widget.Toolbar>

